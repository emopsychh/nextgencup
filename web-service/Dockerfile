# Используем официальный Node.js образ (LTS версия, например, node:lts-slim)
FROM node:lts-slim

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файлы зависимостей
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем все остальные файлы приложения
COPY . .

# Открываем порт 3000, который используется React‑приложением
EXPOSE 3000

# Запускаем приложение в режиме разработки
CMD ["npm", "start"]

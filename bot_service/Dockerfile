# Используем официальный образ Python (например, 3.9-slim)
FROM python:3.9-slim

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файл с зависимостями и устанавливаем их
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Копируем весь остальной код бота в контейнер
COPY . .

# Открываем необходимые порты (если бот использует вебхуки или API, можно указать их)
EXPOSE 8000

# Запускаем приложение (замените bot_main.py на имя вашего основного файла, если требуется)
CMD ["python", "main.py"]
    